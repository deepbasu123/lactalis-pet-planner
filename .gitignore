.venv/
__pycache__/
*.pyc
node_modules/
frontend/dist/
.superpowers/
.env
.DS_Store
.pytest_cache/
.databricks/

# Real operational workbooks (e.g. "PET Traffic Lights *.xlsx") are loaded
# locally by --data-file at deploy time and must never be committed -- only
# the resulting Unity Catalog tables (access-controlled separately) hold
# the real numbers. See backend/excel_source.py.
*.xlsx
*.xls

# Ruflo local secrets and runtime data
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Codex local configuration
.codex/
# Claude Flow runtime data
# Environment variables

# --- ruflo orchestration scaffolding (local only, not part of the app) ---
.agents/
.claude-flow/
.claude/
.mcp.json
.swarm/
ruvector.db
AGENTS.md
CLAUDE.md
